

fn main() {
    //Part of this is for old versions, check it out after the cohort!



    // pub async fn http_text () {
    //     let response_text = reqwest::get("https://en.wikidxfgdf")
    //         .await.expect("Could not make request!")
    //         .text().await.expect("Could not read response text!");
    //      println!("Response Text: {}", response_text);
    
    // }





    // pub async fn http_get () {
    //     match reqwest::get("https://en.wikipedia.org/wiki/Main_Page").await {
    //         Ok(mut response) => {
    //             // Check if 200 Ok
    //             if response.status() == reqwest::StatusCode::OK {
    //                 match  response.text().await {
    //                     Ok(text) => println!("Response Text: {}", text),
    //                     Err(_) => println!("Could not read response text!")
                        
    //                 }
    
    //             } else {
    //                 println!("Response was not 200 OK.");
    //             }
    
    //         },
    
    //         Err(_) => println!("Could not make request!")
    
    //     }

    // }    //Part of this is for old versions, check it out after the cohort!

}
